import { FormRadioButtonField } from './form-radio-button-field';

describe('FormRadioButtonField', () => {
  it('should create an instance', () => {
    expect(new FormRadioButtonField()).toBeTruthy();
  });
});
