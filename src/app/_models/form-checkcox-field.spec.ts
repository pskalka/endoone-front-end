import { FormCheckcoxField } from './form-checkcox-field';

describe('FormCheckcoxField', () => {
  it('should create an instance', () => {
    expect(new FormCheckcoxField("")).toBeTruthy();
  });
});
