import { FormDropdownlistField } from './form-dropdownlist-field';

describe('FormDropdownlistField', () => {
  it('should create an instance', () => {
    expect(new FormDropdownlistField("")).toBeTruthy();
  });
});
